DEBUG=1
SECRET_KEY=
ALLOWED_HOSTS=localhost,127.0.0.1
SURVEY_DB_NAME=bokebi
SURVEY_DB_USER=bokebi
SURVEY_DB_PASSWORD=
CONTACT_DB_NAME=bokebi_contacts
CONTACT_DB_USER=bokebi_contacts
CONTACT_DB_PASSWORD=
# Optional: comma-separated operator addresses. Notifications contain no contact data.
CONTACT_NOTIFICATION_RECIPIENTS=
DEFAULT_FROM_EMAIL=
# Brevo transactional API credentials. The sender must be verified in Brevo.
BREVO_API_KEY=
BREVO_SENDER_EMAIL=
# Optional SMTP settings, used only when EMAIL_BACKEND is overridden.
EMAIL_HOST=localhost
EMAIL_PORT=25
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=0
EMAIL_TIMEOUT=10
